apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
spec:
  type: NodePort
  selector:
    app: result
  ports:
    - name: "result-service"
      targetPort: 80
      port: 80
      nodePort: 31001
